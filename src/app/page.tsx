import Header from "@/components/Header"
import type { FC } from "react"

const Home: FC = () => {
  return (
    <div className="w-full h-screen flex justify-center items-center bg-dark text-white relative">
      <Header />
    </div>
  )
}

export default Home
